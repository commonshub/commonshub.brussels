EmptyDataState from commonshub-brussels. Use via `window.CommonsHub.EmptyDataState` (bundle loaded from the root `_ds_bundle.js`).

Component to display when data directory is empty
Shows helpful instructions for fetching data
